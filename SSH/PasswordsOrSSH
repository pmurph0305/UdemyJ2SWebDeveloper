1. Diffie-Hellman key exchange.
2. Arrive at symmetric key.
3. make sure of no funny business. (hashing)
4. authenticate user.

Auth in SSH:
Can enter a password, but there's a better way.
Set up public/private key pair in cygwin with
ssh-keygen
ssh-copy-id -i root@ip_address
ssh root@ip_address